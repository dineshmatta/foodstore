<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "FoodStore - Home Made food delivered to your doorsteps" %></title>
    <link rel="shortcut icon" href="<%=asset_path("logo.png")%>" >


    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <%= render "layouts/header"%>

    <%= render "layouts/alerts" %>

    <div id="main-content" class="row full-min-height">
      <%= yield %>
    </div>

    <footer class="row">
      <div class="large-12 columns">
        <hr>
        <p>© FoodStore 2014 | <span><%=link_to "Want to become a cook?", new_user_registration_path(role: 'cook'), {:style=>'color:#FFFFFF;', :class => "become-cook"}%></span> | <span><%=link_to "Contact Us", '#', {:style=>'color:#FFFFFF;', :class => "contact-us"}%></span></p>
      </div>
    </footer>

    <%= javascript_include_tag "application" %>
    
  </body>
</html>
